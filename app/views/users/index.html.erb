<% provide(:title, 'All users') %>

<h1>All users</h1>
<div class='row'>
  <% @users.each do |user| %>
    <div class='single-user'>
      <%= gravatar_for user, size: 50 %>
      <%= link_to user.name, user %>
    </div>
  <% end %>
</div>
